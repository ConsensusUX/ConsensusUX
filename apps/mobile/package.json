{
  "name": "@consensusux/mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "expo start",
    "build": "expo build"
  },
  "dependencies": {
    "@consensusux/core": "workspace:*"
  }
}
